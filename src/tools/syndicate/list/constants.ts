export const LIST_SYNDICATES_DESCRIPTION = [
    'Browse the syndicate registry — the player clans that grant trade/transport fee discounts to members and',
    'levy a tax routed to the clan. Filter by name or member-count band, sort (members_desc | recent | name),',
    'and page with limit/offset. Each card carries the four fee rates as percentages, the member count, the',
    'manager address, and the creation time. Public read.',
].join(' ');
