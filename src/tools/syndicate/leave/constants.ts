export const LEAVE_SYNDICATE_DESCRIPTION = [
    'Leave the syndicate you currently belong to. Allowed only once the exit cooldown that started at your join',
    'time has elapsed — an early attempt fails clearly and reports the earliest time you may leave. Once you',
    'leave you may join another syndicate immediately. Writes on-chain (a wallet transaction). Fails clearly if',
    'you are not in any syndicate, or if the registry is not deployed on this network.',
].join(' ');
