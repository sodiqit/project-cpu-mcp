export const GET_SYNDICATE_MEMBERSHIP_DESCRIPTION = [
    'Check whether a wallet belongs to a syndicate — omit address to check your own. A non-member gets a plain',
    '"not a member" answer (not an error). A member gets their join time, the earliest time they may leave, and',
    'the full card of their syndicate (name, manager, the four fee rates as percentages, member count). Public',
    'read.',
].join(' ');
