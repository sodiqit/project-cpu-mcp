export const GET_MARKETS_DESCRIPTION = [
    'Scout the marketplace: one compact row per (Hub, resource) with open-vs-incoming lot counts, lowest',
    'price, and distance. The recommended first look at what is for sale and where — then drill into specific',
    'lots with `cpu_list_lots`. Public read; supports hub / resourceId filters and an optional zone (aroundTokenId',
    'or centerX/centerY + radius).',
].join(' ');
