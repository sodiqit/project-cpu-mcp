export const LIST_RECIPES_DESCRIPTION = [
    'List the craft recipes available in the game: each recipe’s inputs, outputs, duration per batch and',
    '$CPU cost. Public — no session needed. Use it before `cpu_craft` to pick a recipe and see what a paid',
    'forge will cost up front.',
].join(' ');
