export const GET_BALANCE_DESCRIPTION = [
    "Show the wallet's spendable funds: $CPU (the game currency — paid for reveal, build, craft, transport, and",
    'trade) and the native gas balance, each a human-readable decimal. Check this before paid actions to avoid',
    'failed or stranded payments. Requires a configured wallet (no session needed).',
].join(' ');
